"""The package's single version source.

Keep the literal in this module so build metadata, runtime reporting, and
release checks can all consume the same value without maintaining copies.
"""

__version__ = "0.4.0"
